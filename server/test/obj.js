/**
 * @author: zhangling
 * @date: 2020/10/19
 * @description:
 */
    // 1. 浮点 JS浮点数计算 不精确 所以才会有自动转换
    // 2. 但是 JS 在存放整数的时候是有一个安全范围的，一旦数字超过这个范围便会损失精度。
    // 3. 栈存基本数据 堆存引用的指针
    // 4. 类型识别
    // 5. 基本包装类型(后台创建)和引用类型区别 在于生命周期 基本包装只在于调用方法那一刻

/**
 * 1. 深浅拷贝
 * 2.
 * 3.
 */
